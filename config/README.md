# config

This directory contains the configuration that defines Churchill.

Rather than modifying files directly in a user's home directory, Churchill keeps its configuration here as the single source of truth. During installation, these files are copied into the appropriate locations on the system.

Every configuration file is documented and organised to be readable, editable, and easy to troubleshoot.
